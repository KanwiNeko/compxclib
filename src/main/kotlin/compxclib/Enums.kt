package compxclib

enum class SqrtBranch {
    POSITIVE, NEGATIVE
}

enum class Parameter {
    REAL, IMAGINARY, BOTH
}

enum class Sign {
    POSITIVE, NEGATIVE
}

enum class Operator {
    PLUS,
    MINUS,
    TIMES,
    DIVIDE,
    MODULO,
    POWER
}